<!-- POLYMAI:START -->
# Polymai app - Claude rules

This repository was generated by Polymai. Treat it as an existing product, not a disposable scaffold.

## Definition of Done
Before your final response:
1. Read `POLYMAI_RULES.md`, `POLYMAI_PLAYBOOK.md` if present, `registry.json`, and `CHANGELOG.md` if present.
2. Run `node POLYMAI_CHECKS.cjs` and repair relevant REPAIR NEEDED items without reducing product usefulness.
3. Update `registry.json` so it reflects repository reality: loadOrder paths exist, app source files are represented, routes/mounts/dataAccess/capabilities match meaningful code changes.
4. Append one compact `CHANGELOG.md` entry with Changed, Files, Learning, Checks, and Risk.
5. If `POLYMAI_PLAYBOOK.md` contains Project Pattern Memory and you implement or touch the matching feature, preserve that pattern and include only the applicable hint in the changelog Learning field.

## Claude Hook
The project-local Claude hook in `.claude/settings.json` is warn-only. It may print Polymai check output at stop time, but it must not be treated as a hard blocker or a reason to delete useful app behavior.

## Non-negotiables
- Never put secrets in frontend files, registry.json, data files, SQL, or published assets.
- Keep Supabase schema.sql, policies.sql, data/schema.json, and runtime calls aligned when Supabase is used.
- Preserve working behavior unless the user explicitly asks to replace it.
- Keep Polymai instruction/check files out of registry.json loadOrder, app UI, runtime imports, and GitHub-published output.
<!-- POLYMAI:END -->
